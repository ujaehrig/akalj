spring:
  application:
    name: akalj
  redis:
    host: ${REDIS_HOST}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASS}


management:
  endpoint:
    health:
      show-details: always
  server:
    port: 8081

akalj:
  garbage-types:
    BIO:
      - Bioabfall
    RESTMUELL:
      - Restmüll
    PAPIER:
      - Papier
    WERTSTOFF:
      - Wertstoff

